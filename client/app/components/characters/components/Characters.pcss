@import '../../../styles/variables.pcss';

.characters {
  width: 100%;
  lost-flex-container: row;
  overflow: hidden;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  min-height: 100vh;
}
